# immediate operations (non-zero immediate)
# floating-point operations (fp arguments follow opcode)
# integer operations (int arguments follow opcode)

# ISA 
# instruction (case sensitive)    0x(opcode in hex)
